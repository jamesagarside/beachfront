# Issue tracker

Issues for this repo live in **GitHub Issues** on `jamesagarside/beachfront`, managed
with the `gh` CLI.

External pull requests are **not** a triage surface for now — triage operates on
issues only. Revisit if the project starts taking feature requests as PRs.
